```sql
SELECT L.HOUR, COUNT(A.ANIMAL_ID) AS COUNT
    FROM (SELECT LEVEL-1 AS HOUR
          FROM DUAL
          CONNECT BY LEVEL<=24) L
    LEFT OUTER JOIN ANIMAL_OUTS A
    ON L.HOUR = TO_NUMBER(TO_CHAR(A.DATETIME,'HH24'))
    GROUP BY L.HOUR
    ORDER BY L.HOUR;
```

