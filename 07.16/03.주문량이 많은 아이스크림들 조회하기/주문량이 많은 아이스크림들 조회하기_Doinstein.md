```SQL
SELECT FLAVOR
FROM (SELECT FLAVOR,RANK() OVER (ORDER BY SUM(TOTAL_ORDER) DESC) AS RANK
    FROM ((SELECT * FROM FIRST_HALF)
            UNION ALL
          (SELECT * FROM JULY))
    GROUP BY flavor)
WHERE RANK <= 3;
```

