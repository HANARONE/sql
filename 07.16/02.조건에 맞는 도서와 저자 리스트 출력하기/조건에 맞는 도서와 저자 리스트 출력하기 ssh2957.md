```SQL
SELECT A.BOOK_ID, B.AUTHOR_NAME,TO_CHAR(PUBLISHED_DATE,'YYYY-MM-DD') PUBLISHED_DATE
FROM BOOK A JOIN AUTHOR B ON A.AUTHOR_ID=B.AUTHOR_ID
-- JOIN해주고 
WHERE A.CATEGORY='경제'
-- 경제뽑아주기
ORDER BY PUBLISHED_DATE
```

