```sql
SELECT WAREHOUSE_ID, WAREHOUSE_NAME, ADDRESS ,  NVL(FREEZER_YN,'N')
#NVL 함수를 써서 풀었습니다.
from FOOD_WAREHOUSE
WHERE  SUBSTR(ADDRESS,1,3)='경기도'
# SUBSTR 함수를 쓰면 원하는 부분만 맞으면 추출가능
ORDER BY WAREHOUSE_ID 
```

