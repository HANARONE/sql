```SQL
SELECT  TRUNC(PRICE,-4) AS PRICE_GROUP, COUNT(PRODUCT_ID) AS PRODUCTS
-- 앞에 입양하기 문제를 풀어서 쉽게 생각해 낼수있었다.
-- 그냥 앞자리만 남기고 버려버리면 그룹화끝
FROM PRODUCT
GROUP BY TRUNC(PRICE,-4)
ORDER BY TRUNC(PRICE,-4)
```

