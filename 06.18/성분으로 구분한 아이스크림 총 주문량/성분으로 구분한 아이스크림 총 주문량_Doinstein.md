```sql
SELECT I.INGREDIENT_TYPE, SUM(F.TOTAL_ORDER) AS TOTAL_ORDER -- 성분 타입별 주문합
    FROM FIRST_HALF F
    JOIN ICECREAM_INFO I
    ON F.FLAVOR = I.FLAVOR -- FLAVOR을 기준으로 inner join
    GROUP BY INGREDIENT_TYPE -- 성분 타입별 그룹화
    ORDER BY TOTAL_ORDER;
```

