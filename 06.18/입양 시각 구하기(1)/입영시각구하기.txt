-- 코드를 입력하세요
SELECT HOUR, COUNT(HOUR) FROM (SELECT ANIMAL_ID, ANIMAL_TYPE, TO_NUMBER(TO_CHAR(DATETIME, 'HH24')) AS HOUR, NAME, SEX_UPON_OUTCOME FROM ANIMAL_OUTS) GROUP BY HOUR HAVING HOUR >= 09 AND HOUR <= 19  ORDER BY HOUR ASC