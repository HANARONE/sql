```sql
SELECT MEMBER_ID, MEMBER_NAME, GENDER, TO_CHAR(DATE_OF_BIRTH, 'YYYY-MM-DD') AS DATE_OF_BIRTH
--기본적인 틀 만들기 ft: 이제는 익숙한 TO_CHAR
FROM MEMBER_PROFILE 
WHERE GENDER='W' AND TO_CHAR(DATE_OF_BIRTH, 'MM')=03
--W 빼오고 03달만 빼온다~
AND TLNO!= 'NULL' 
--NULL값인 애들은 제외시켜준다~
ORDER BY MEMBER_ID ASC
```

