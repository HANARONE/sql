```
SELECT USER_ID, PRODUCT_ID
    FROM ONLINE_SALE
    GROUP BY USER_ID, PRODUCT_ID -- USER_ID와 PRODUCT_ID 그룹화
    HAVING COUNT(PRODUCT_ID) >=2 -- PRODUCT_ID 구매 개수가 2이상(재구매)인것
    ORDER BY USER_ID, PRODUCT_ID DESC;
```